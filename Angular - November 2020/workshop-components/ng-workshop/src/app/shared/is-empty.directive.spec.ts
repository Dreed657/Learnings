import { IsEmptyDirective } from './is-empty.directive';

describe('IsEmptyDirective', () => {
  it('should create an instance', () => {
    const directive = new IsEmptyDirective();
    expect(directive).toBeTruthy();
  });
});
